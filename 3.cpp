#include <stdio.h>


void Parity2numbers(int t)
{
	int a, b;
	printf("nhap vao 2 so a, b:\n ");scanf("%d%d", &a, &b);
	
	if(a % 2 == 0)
	{
	printf("%d la so chan  \n", a);
	}else{
		printf("%d la so le  \n",a);
	}
	
	if( b % 2 == 0 )
		{
		printf("%d la so chan \n ", b);
		}else{
			printf("%d la so le  \n", b);
		}
		
		if (a, b % 2 == 0 )
			{
			printf("cung chan\n", a,b);
			}else{
				printf("cung le\n", a,b);
				 }
	
}
		
		

